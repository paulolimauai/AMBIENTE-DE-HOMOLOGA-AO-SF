services:
  - type: web
    name: sistema-financeiro
    runtime: node
    plan: free
    buildCommand: npm install --no-audit --omit=optional || npm install --no-audit
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
